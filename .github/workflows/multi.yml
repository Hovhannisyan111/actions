name: Multi-job

on: [push]

jobs:
  lint:
    runs-on: ubuntu-latest
    steps:
      - name: Do some work
        run: echo Working...

  mlint:
    runs-on: ubuntu-latest
    needs: lint
    steps:
      - name: Dont know
        run: |
          ls
          pwd
  jlint:
    runs-on: ubuntu-latest
    needs: lint
    steps:
      - name: Dont know
        run: echo some

  olint:    
    runs-on: ubuntu-latest
    needs: [ mlint, jlint ]
    steps:
      - name: Dont know
        run: echo Finish
