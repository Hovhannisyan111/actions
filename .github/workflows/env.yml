name: Env

on:
  push:
    branches:
      - main

jobs:
  env-menv:
    runs-on: ubuntu-latest
    steps:
      - name: print
        env:
          MY_ENV: action
        run: echo ${{ env.MY_ENV }}
